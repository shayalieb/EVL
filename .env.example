# Base URL of the backend API (see server/). Points at local dev by default;
# set this to the deployed Railway URL (e.g.
# https://evl-production.up.railway.app/api) for production builds, or to
# test the local frontend against the deployed backend.
VITE_API_BASE=http://localhost:4000/api
